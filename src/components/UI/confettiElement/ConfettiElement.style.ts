import styled from "styled-components";

export const ConfettiElementStyle = styled.figure`
  pointer-events: none;
  user-select: none;
  position: absolute;
  z-index: 0;
  width: calc(100% + 2rem);
  top: -35px;
`;
